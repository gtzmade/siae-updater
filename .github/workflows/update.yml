name: Actualizar informe de licenciaturas

on:
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 1️⃣ Ejecutar el scraper
      - name: Ejecutar scraper (tolerante)
        run: |
          python scraper.py || echo "⚠️ scraper.py falló, pero se continúa con el flujo."

      # 2️⃣ Limpiar snapshot
      - name: Clean snapshot (tolerante)
        run: |
          python clean_snapshot.py || echo "⚠️ clean_snapshot.py falló, pero se continúa con el flujo."

      # 3️⃣ Actualizar Excel con diferencias
      - name: Diff & update Excel (tolerante)
        run: |
          python diff_and_update.py || echo "⚠️ diff_and_update.py falló, pero se continúa con el flujo."

      # 4️⃣ Crear Release pública con timestamp
      - name: Crear Release y subir el Excel
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TS=$(date +"%Y%m%d-%H%M%S")
          OUT="PE_licenciatura_${TS}.xlsx"
          echo "📦 Archivo de salida: $OUT"
          echo "tag-pe-${TS}" >> $GITHUB_OUTPUT
          git add .
          git commit -m "auto: actualización semanal de planes de estudio" || echo "No hay cambios para commit"
          git push || echo "No hay cambios para subir"

          gh release create "pe-${TS}" $OUT --title "PE Licenciatura – ${TS}" --notes "Actualización automática de planes de estudio" --repo $GITHUB_REPOSITORY --latest

      # 5️⃣ Enviar correo de notificación con link público
      - name: Enviar correo
        env:
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
          FROM_PASSWORD: ${{ secrets.FROM_PASSWORD }}
          TO_EMAIL: ${{ secrets.TO_EMAIL }}
        run: |
          python - <<'EOF'
          import smtplib
          from email.mime.text import MIMEText

          from_email = "${{ secrets.FROM_EMAIL }}"
          to_email = "${{ secrets.TO_EMAIL }}"
          password = "${{ secrets.FROM_PASSWORD }}"

          subject = "SIAE: Cambios detectados en planes de estudio"
          body = """\
          <html>
            <body>
              <p>Hola 👋<br><br>
              Se detectaron <b>cambios</b> en el catálogo de planes de estudio.<br><br>
              Se generó un nuevo Excel que ya está disponible públicamente en la última Release.<br><br>
              👉 <a href="https://github.com/gtzmade/siae-updater/releases/latest">Ver última Release</a><br><br>
              — Bot SIAE
              </p>
            </body>
          </html>
          """

          msg = MIMEText(body, 'html')
          msg['Subject'] = subject
          msg['From'] = from_email
          msg['To'] = to_email

          with smtplib.SMTP_SSL("smtp.gmail.com", 465) as server:
              server.login(from_email, password)
              server.send_message(msg)

          print("📧 Correo enviado exitosamente.")
          EOF
